// • if else yapısıyla basit bir örnek kodlayın.

let vizeNot = 40;
let finalNot = 75;

let sonuc = vizeNot * 0.3 + finalNot * 0.7;

if (sonuc >= 50) {
  console.log(`Ortalamanız: ${sonuc} Dersten Geçtiniz.`);
} else {
  console.log(`Ortalamanız: ${sonuc} Dersten Kaldınız.`);
}
